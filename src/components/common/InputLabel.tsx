import styled from 'styled-components'
import { INPUT_LABEL_COLOR } from '../../constants/styleConstants'

export const Label = styled.span`
    letter-spacing: 0px;
    color: ${INPUT_LABEL_COLOR};
`
